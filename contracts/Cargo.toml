[workspace]
resolver = "2"
members = [
    "counter",
    "vault",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1

[patch.crates-io]
ethnum = { path = "local-ethnum" }

